version: "3"

services:
  front:
    build: front
    image: otomato/istio-games-front:${TAG-latest}
    ports:
      - "5000:80"
    
  aleph:
    build: aleph
    image: otomato/istio-games-aleph:${TAG-latest}

  beth:
    build: beth
    image: otomato/istio-games-beth:${TAG-latest}

  mongo:
    image: mongo

 

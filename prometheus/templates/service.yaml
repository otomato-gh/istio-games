apiVersion: v1
kind: Service
metadata:
  name: prometheus-server
  labels:
    app: prometheus
    component: server
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9090
  selector:
    app: prometheus
    component: server